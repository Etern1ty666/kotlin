org.gradle.parallel=true
org.gradle.caching=true

# The lesson tasks hand System.in to the running program so you can type
# answers to readLine(). That is not compatible with the configuration cache,
# and this build is far too small to benefit from it anyway.
org.gradle.configuration-cache=false
